// Ambient module declarations for side-effect imports.
// TypeScript 5.6+ requires these for files without their own type declarations
// (e.g. `import './globals.css'` in app/layout.tsx).
declare module '*.css';
