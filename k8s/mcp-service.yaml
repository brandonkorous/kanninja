apiVersion: v1
kind: Service
metadata:
  name: mcp
  namespace: kanninja
  labels:
    app.kubernetes.io/name: mcp
    app.kubernetes.io/part-of: kanninja
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
